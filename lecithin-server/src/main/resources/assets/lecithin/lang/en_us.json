{
  "lecithin.compat-config.async-context-inheritance.comment": "Let an async Bukkit task remember the execution context of the thread that created it, so a\nlegacy sync scheduler call made from inside that task can still be redispatched.\nThe owner is captured in CraftAsyncTask's constructor, which still runs on the thread that\nasked, so it is a fact at that moment rather than something deduced later. Inheritance stops\nafter one hop and the thread's own context always wins when it has one.\nDisabling this makes a legacy sync call from an async task fail closed again, exactly as on\nstock Folia. Requires caller-context-dispatch.",
  "lecithin.compat-config.async-event-provenance.comment": "Let an asynchronous Bukkit event that names exactly one online player stand as the execution\nprovenance for legacy sync scheduler calls made from inside its listeners; the task then goes\nto that player's EntityScheduler, which is the documented API for scheduling one entity's work\nfrom any thread. Paper dispatches AsyncPlayerChatEvent on a pooled thread that owns nothing, so\nwithout this such a call has nowhere to go and takes the whole handler down with it.\nDisabling this restores stock Folia rejection for those calls; no other dispatch path changes.\nRequires caller-context-dispatch.",
  "lecithin.compat-config.async-platform-event-global-scope.comment": "The weaker half of async-event-provenance: an asynchronous event the platform itself defines\nbut which names no entity at all - the connection phase above all - is treated as server-scope,\nand its legacy sync calls go to the global region. Kept separate so the strong claim (an event\nthat names one player belongs to that player) can stay on while this one is turned off.\nThe platform-package check is what stops this from becoming \"any async caller gets the global\nregion\": a plugin firing its own async event is still refused.\nDisabling this makes those calls fail closed again. Requires async-event-provenance.",
  "lecithin.compat-config.caller-context-dispatch.comment": "Run a sync Bukkit scheduler task that Folia rejects (scheduleSyncDelayedTask, runTask,\nrunTaskLater, callSyncMethod) on whatever the calling thread was already working on: a region\nthread goes to that region's scheduler, the global region thread to the global scheduler, and\nan entity-scoped context to that entity's scheduler. Nothing is inferred from the Runnable, and\na thread with no observable context is still rejected.\nA task that reaches into state it does not own still fails loudly at that access; what changes\nis only that a task which was never given a chance to run now runs.\nDisabling this restores stock Folia rejection, which propagates out of the plugin's event\nhandler or onEnable and aborts everything after the call.",
  "lecithin.compat-config.command-dispatch-handover.comment": "Hand a console/RCON-sender Bukkit.dispatchCommand made from a region tick thread to the global\nregion, instead of refusing it where it was called. Folia already states that the global region\nis where a console sender executes; this sends it exactly there. An Entity sender is untouched.\nThe parse stays on the calling thread, so Paper's only meaningful false (no such command) is\nstill returned synchronously; only the execution is deferred.\nNot preserved: an exception thrown during execution no longer propagates out of the\ndispatchCommand call - it surfaces on the global region on a later tick.\nDisabling this makes such a dispatch throw out of the plugin's event handler again.",
  "lecithin.compat-config.comment": "Compatibility with plugins and APIs written for a single main thread.\nEvery entry here is a separate switch over one specific gap between Paper and regionised\nthreading, and every one of them can be turned off individually to get stock Folia behaviour\nback. None of them relaxes a thread-ownership or region check: work that genuinely touches\nstate it does not own still fails, loudly, at the access itself.",
  "lecithin.compat-config.cross-region-block-load.comment": "Load the chunk a read-only block lookup needs, instead of refusing the read because nobody had\nasked for that chunk yet. Paper's block read loads its own chunk synchronously, so a plugin\nreading the world spawn in onEnable, or inspecting a world it just created, always gets an answer;\nhere that chunk may not exist yet and the read would throw instead.\nOnly from a thread that is ticking no region at all - the bootstrap thread during startup, and the\nglobal region thread, which owns no chunks in any world and so has no other thread the caller\ncould have used. It uses the platform's own syncLoadNonFull at FULL.getParent(), which is what\nFolia itself calls from PlayerSpawnFinder on that same thread.\nDeliberately NOT from a region tick thread, and that is a measured limit rather than caution:\nsyncLoadNonFull processes ticket updates on the target world's holder manager, which asserts that\nworld against the one the calling thread is ticking. A cross-world load from a region thread\ntherefore always throws World check failed - after it has added its chunk ticket and before it\nremoves it, and that ticket has no timeout, so every such read used to leak one permanently\npinned chunk. The leak is also what hid it: the chunk stayed resident, so the second read of the\nsame block succeeded.\nChunks that are already resident are unaffected and are still answered on any tick thread; that is\nthe path a loaded destination world actually takes.\nThe cost where it does run: the calling thread does nothing else until the load finishes, and on\nthe global region the watchdog will say so past 5s. Paper stalls every region for the same read,\nbecause it has only one.\nDisabling this narrows cross-region-block-read to chunks that are already resident, on every\nthread. Requires cross-region-block-read.",
  "lecithin.compat-config.cross-region-block-read.comment": "Answer a read-only block lookup from an already-loaded chunk when the calling tick thread does\nnot own that chunk's region, instead of refusing it. Every world builds its own regioniser, so\na thread ticking world A can never be the tick thread for a chunk in world B - a cross-world\nread is not merely inconvenient but structurally impossible to satisfy otherwise.\nOnly resident chunks are served; nothing here can load, generate or tick a chunk, writes are\nuntouched, and threads that are not tick threads at all still fail so the async catcher keeps\nworking. The worst outcome is a block state one tick stale.\nDisabling this restores the stock refusal.",
  "lecithin.compat-config.diagnostics.comment": "Report Bukkit scheduler calls that Folia rejects as one attributable log record instead of a\nmessage-less UnsupportedOperationException: plugin name and version, jar file and SHA-256, the\nscheduler method actually called, delay/period, the sync/async flag, the plugin callsite and the\nthread context - once per distinct callsite. Also covers the once-per-callsite notices for a\ndeferred teleport and a successful context redispatch.\nScheduling behaviour is unchanged either way; the call still throws.\nDisabling this leaves the bare exception with no attribution.",
  "lecithin.compat-config.economy-serialization.comment": "Restore the per-account serialization Paper provided implicitly by having one main thread, for\nplugin economy ledgers that keep a balance in a plain field and update it with a non-atomic\nread-modify-write. On two region threads those updates interleave and one is silently lost -\nwith no exception and no ownership violation, because a money field is not world state.\nIt hooks two boundaries the server owns: Bukkit's ServicesManager (every Vault consumer resolves\nthe same provider object) and the legacy plugin command path. Different accounts still run fully\nin parallel; no plugin bytecode is touched, and if the wrapping fails the original provider is\nregistered unchanged.\nDisabling this leaves those ledgers racing.",
  "lecithin.compat-config.foreign-world-ticket-updates.comment": "Do not try to drain another world's pending chunk full-status updates.\nChunkHolderManager.processTicketUpdates finishes with \"if this is a tick thread, drain the pending\nfull-status queue\", and that drain resolves the region the thread is ticking and asserts its world\nagainst the manager's. The condition asks the wrong question: the drain needs a tick thread OF\nTHIS WORLD, not any tick thread. The line immediately above it already asks the right one.\nNothing in the platform reaches that path for a foreign world, so upstream never trips it - but a\ncross-world chunk load does. syncLoadNonFull adds a chunk ticket, calls processTicketUpdates on\nthe target world, and waits; from a region of another world it threw World check failed between\nadding the ticket and removing it, and it has no finally, so the ticket stayed forever. Every\ndistinct chunk read that way pinned one chunk holder and one region section in the target world\nfor the life of the server - and the leak is what hid the bug, because the chunk stayed resident\nand the second read of the same block succeeded.\nSkipping is the correct answer, not merely a quieter one: the queue belongs to the other world's\nregion and would be applied with this world's bookkeeping, which is exactly what the assertion\nexists to prevent. The target world's own regions drain their own queues when they tick, so\nnothing is dropped.\nDisabling this restores the bare tick-thread condition, and with it the exception and the leak.",
  "lecithin.compat-config.paper-lib-environment.comment": "Give an embedded PaperLib copy handlers that match this platform. PaperLib parses the server\nversion with a regex whose major-version group is a single digit, so on 26.2 the match fails,\nthe parsed version is 0, and it keeps its synchronous chunk and teleport fallbacks - the two\ncalls that cannot work across a region boundary. This is an upstream defect in PaperLib and it\nmisfires the same way on stock Paper 26.2.\nIdentification is structural (the class must be shaped like PaperLib), no bytecode is modified,\nBukkit.getVersion() is untouched, and PaperLib.isVersion(...) still returns its own honest answer.\nDisabling this leaves every embedded copy with whatever handlers it selected for itself.",
  "lecithin.compat-config.permission-locking.comment": "Take the class monitor in the two PaperPermissionManager methods Folia's synchronization sweep\nmissed: getDefaultPermissions(boolean), whose ImmutableSet.copyOf sizes an array and then fills\nit - so a concurrent registration from another thread aborts the copy with\nArrayIndexOutOfBoundsException - and removePermission(String). Two threads really do register\npermissions during boot, and the server died there roughly one start in four.\nOnly a set copy runs under the monitor, so no lock-ordering edge is added.\nDisabling this restores the unsynchronized bodies and the boot-time crash with them.",
  "lecithin.compat-config.region-read-diagnostics.comment": "Make an off-region block read fail attributably. Level#getBlockStateIfLoaded dereferences\ngetCurrentWorldData() with no null check, so a cross-region read ends in a\nNullPointerException naming an unrelated field (captureTreeGeneration) rather than in the\nthread-ownership message, which carries thread context, world and position.\nThe read already failed and still fails, at the same place, for the same reason; only the\nexception's identity and diagnostics change.\nDisabling this restores the misleading NullPointerException.",
  "lecithin.compat-config.respawn-location-lookup.comment": "Return a player's stored respawn location when the calling thread may not read the respawn world\nto validate the spawn block, instead of throwing World mismatch out of the caller.\nThe respawn world comes from the player's persisted respawn data and is unrelated to the region\nthe caller is ticking, so validating it fails whenever a player's bed is in another world - which\nis every cross-world move, because PlayerChangedWorldEvent fires on the destination region and\nreading a bed spawn from there is a standard Paper idiom. OfflinePlayer is covered the same way.\nThis consults the platform's own ownership check rather than bypassing it; nothing is loaded,\ndispatched or waited on, and a caller that is not on a tick thread at all still fails loudly, as\nit does today.\nThe fallback is the common path rather than an edge case: a bed in another world can never be\nvalidated from a region of this one, so every cross-world lookup takes it. On that path the answer\nis not validated, so a bed broken since the player last slept in it is reported as a respawn\nlocation rather than as null - and the coordinate is the raw stored block corner, with no centring\nand no safety check, so teleporting straight to it can put the player inside a block where null\nwould have sent them to world spawn instead.\nDisabling this restores the unconditional validating read, and the exception with it.",
  "lecithin.compat-config.restore-async-scheduler.comment": "Restore Bukkit's asynchronous scheduler. Folia replaces CraftScheduler.handle with an\nunconditional throw, which also killed the async path - and that path never needed a main\nthread, since Paper already runs those tasks on CraftAsyncScheduler's own pool. Sync tasks are\nstill rejected here; async tasks that illegally touch world state are still caught by the\nthread-ownership checks.\nAlso restores the two guarantees the async path depends on: BukkitTask.cancel() no longer\nthrows after the cancel has taken effect, and disabling a plugin cancels its tasks again -\nwithout that, a disabled plugin's async timers keep running its code until shutdown.\nDisabling this makes every Bukkit scheduler call throw, async included.",
  "lecithin.compat-config.riding-teleport.comment": "Restore Paper's answer to teleporting an entity that is riding something else: dismount that\ntarget, then move it, leaving the vehicle and any other passengers alone. Entity#teleportAsync\nrefuses outright when the target is a passenger and the Bukkit API always passes the passenger\nflag, so every API teleport of a rider - boats, minecarts, horses, seats, mounts - returns false\nand does nothing, and most callers discard the return value and cannot even notice.\nA target whose teleport event is then cancelled is put back on its vehicle, like Paper.\nDisabling this restores the platform's blanket refusal.",
  "lecithin.compat-config.scoreboard-api.comment": "Open the five Bukkit scoreboard methods Folia blocks with an unconditional throw, under region\nrules rather than single-main-thread rules: plugin-created boards may be built and changed from\nany tick thread (their broadcast path is thread-safe), the main scoreboard is persisted global\nstate so structural changes require the global tick thread, and assigning a board to a player\nrequires the thread that owns that player.\nNeither check guesses an owner; both verify the current thread and fail loudly with attribution\nwhen it is the wrong one.\nDisabling this makes the five methods throw again.",
  "lecithin.compat-config.server-current-tick.comment": "Restore the MinecraftServer.currentTick field Folia commented out, advanced once per global\nregion tick - the successor of Paper's main-thread loop, and its single writer. Plugins that\nread it reflectively from their NMS adapters (FastAsyncWorldEdit does) otherwise die on\nNoSuchFieldError and take every one of their commands with them.\nDisabling this removes the field's update; the field itself is only meaningful while this is on.",
  "lecithin.compat-config.startup-context-dispatch.comment": "Treat the server bootstrap thread as global-region provenance for legacy sync scheduler calls,\nso a plugin scheduling work from onEnable reaches the global region instead of being refused.\nRegion ticking has not started at that point, and the global region is where that work would run\nonce it does.\nDisabling this makes such calls fail closed. Requires caller-context-dispatch.",
  "lecithin.compat-config.startup-global-context.comment": "Let the server bootstrap thread satisfy the global-region ownership check while the server is\nstill starting up. Region ticking only begins after initServer(), which is where world loading\nand plugin onLoad/onEnable happen, so during startup the bootstrap thread is the sole owner of\nglobal server state - exactly what Paper's main thread is at the same point. That thread is\nitself a tick thread, so Bukkit.isPrimaryThread() already answers true on it and a plugin asking\nthe standard question was told yes and then rejected anyway.\nCalls made after startup are rejected exactly as before.\nDisabling this restores the stock rejection.",
  "lecithin.compat-config.teleport-handover.comment": "Let a teleport wait for the entity's previous teleport to finish handing it over to another\nregion, instead of being silently refused. A cross-region teleport does not move the entity, it\nbuilds a copy and hands it to the destination region, which adds it to its lookup on a later\ntick; in that window canTeleportAsync() refuses, so a plugin teleporting the same entity twice\nin one tick gets true and then false and the second teleport never happens. Paper does both.\nThe teleport is scheduled on the entity's own EntityScheduler, which the platform transfers with\nthe entity; no owner is guessed and the calling thread does not wait.\nDisabling this restores the plain refusal.",
  "lecithin.compat-config.teleport-refusal-diagnostics.comment": "Print the four predicates behind a refused same-tick second teleport, plus removal reason,\ncallback class, ownership, both chunk states and the vehicle graph - and separately, the level\ncallback's NULL transitions. hasNullCallback() has no Bukkit API, so without this the reason for\nthe refusal cannot be read from outside.\nOutput only: it is called after the platform has already decided to refuse and returns void, so\nit cannot influence the outcome. Prints on a refusal or a transition, never per tick.\nOff by default; turning it on costs log volume only.",
  "lecithin.compat-config.teleport-semantics.comment": "Restore Entity#teleport(Location) and Player#teleport(Location) at the Bukkit API boundary.\nFolia replaces both with an unconditional throw - not \"throws when unsafe\", but always, even\nwhen the destination is a loaded chunk the calling thread already owns. The call is delegated to\nthe platform's own teleportAsync and its future is read without waiting: already completed means\nFolia did the move inline and the real answer is returned, exactly as on Paper; not completed\nmeans another region or world and the teleport was accepted but has not happened yet, which is\nreported as true with a once-per-callsite notice.\nNo ownership check is bypassed and nothing is sent to the global scheduler.\nDisabling this restores the unconditional throw from both classes.",
  "lecithin.event-config.comment": "Bukkit events that Folia routes around and never fires.\nThese are notifications Paper plugins already listen for; restoring them changes which handlers\nare called, not how the teleport itself is performed.",
  "lecithin.event-config.move-rollback-without-teleport.comment": "Put a player back with a position correction, not a teleport, when PlayerMoveEvent is cancelled.\nPaper's rollback is internalTeleport: a position packet and no Bukkit event. Folia replaced it with\nteleportAsync(from, PLUGIN), which is a real teleport. So every cancelled move reports a PLUGIN\nteleport Paper never reported, and - the reason this matters - that teleport is itself cancellable,\nso a second plugin cancelling PLUGIN teleports defeats the rollback and the player keeps the\nmovement the first plugin refused.\nThe correction only writes position and rotation and performs no region handover, so it is used\nonly when this thread owns the destination, which for a rollback to the player's own previous\nposition is the normal case. When it does not, the stock teleport path is used unchanged.\nDisabling this always uses the stock path.",
  "lecithin.event-config.passenger-teleport-cross-world-offset.comment": "For a cross-world teleport, report each carried passenger's own offset position in its event\nrather than the vehicle's, which is what Paper does there. Same-world teleports report the\nvehicle's from/to on both platforms and are unaffected.\nDisabling this gives every passenger the vehicle's position on cross-world teleports too - a\nregistered, harmless divergence from Paper.\nRequires passenger-teleport-events.",
  "lecithin.event-config.passenger-teleport-events.comment": "Fire the teleport event for each passenger a teleported vehicle carries along. The platform\nmoves the whole tree but only the entity actually passed to teleportAsync gets an event, so a\nplayer sitting in a boat, a minecart, a horse or a plugin seat crosses the world with no\nteleport event at all - nothing that vetoes teleports is consulted and anything watching sees an\nunexplained jump.\nThese events are notifications, not veto points: Paper ignores cancellation and setTo for a\ncarried passenger, and so does this, because honouring one passenger's veto could only produce a\nhalf-moved tree.\nDisabling this leaves the carried passengers silent again.",
  "lecithin.event-config.portal-events.comment": "Fire the Bukkit portal and teleport events for a vanilla portal. Folia rewrote Entity#handlePortal\nto route through portalToAsync, which reaches placeInAsync without ever calling teleportAsync and\nwithout ever calling Portal#getPortalDestination - so both places these events were fired from are\ndead code, and PlayerPortalEvent and EntityPortalEvent are dead API on this fork.\nMeasured with the same probe on both platforms, a player without access to the destination world\nwalking into a nether portal: Paper fires PlayerPortalEvent, the access plugin cancels it and the\nplayer stays; this fork fired nothing and the player crossed. Every other teleport path already\nmatched Paper in the same run - the portal was the only hole, and it is how players actually\nchange world.\nCancellation, setTo, setSearchRadius, setCreationRadius and setCanCreatePortal are all honoured.\nPlayerPortalEvent's destination is the approximate scaled exit, which is exactly what Paper passes\nit. PlayerTeleportEvent's is that same approximate exit rather than the resolved portal exit,\nbecause the search finishes on another region after the entity has been detached, where a cancel\ncould no longer be honoured; world, cause and cancellation are exact.\nDisabling this stops all of them again.",
  "lecithin.event-config.respawn-event.comment": "Fire PlayerRespawnEvent, which this fork never fires at all.\nPaper fires it from ServerPlayer#findRespawnPositionAndUseSpawnBlock0, the one place that turns a\nplayer's stored respawn data into a position. Folia's ServerPlayer#respawn is a rewrite that\nresolves the position itself and never calls that method, so the event is dead API here: it is\nnever constructed, and every plugin that places a player after death is simply not called.\nIt is fired the instant the position is final and before anything is applied, on the region thread\nthat owns that position - so a handler's setRespawnLocation is honoured, including into another\nworld, and bed, respawn anchor, missing respawn block and plain world spawn all report the same\nflags Paper reports.\nPaper also consumes a respawn anchor's charge after the event and only if the location was not\nchanged (SPIGOT-5989). This fork builds that step and never runs it, so an anchor is never\ndepleted; restoring the event restores the rule that governs it.\nOne bounded difference: Paper aborts a respawn when the player disconnects while handlers run. It\ncan, because it has not removed them from the world yet - here they already have been, so the\nplacement always completes and the disconnect is handled by the normal path.\nDisabling this stops the event again, and with it the anchor consumption.",
  "lecithin.event-config.spawn-location-event.comment": "Fire the legacy PlayerSpawnLocationEvent during the configuration phase.\nFolia short-circuits the only block that constructs it with a literal false, so a plugin cannot\nchange where a player appears before they join. The plumbing the block needs is all still there and\nstill used - the platform reads back the very player instance the block creates - so this is\ndisabled, not unsupported.\nIt runs from the connection tick on the global region thread, which is a tick thread, so\nBukkit.isPrimaryThread() is true here exactly as on Paper's main thread. Paper's newer\nAsyncPlayerSpawnLocationEvent still runs afterwards and still sees this event's result, which is\nPaper's own ordering.\nDisabling this returns the legacy event to being dead API; the async event is unaffected.",
  "lecithin.event-config.teleport-events.comment": "Fire PlayerTeleportEvent, EntityTeleportEvent and PlayerChangedWorldEvent, which Folia left as\na TODO when it routed every teleport through Entity#teleportAsync. Measured on the same script:\nPaper fires PlayerTeleportEvent 3x and PlayerChangedWorldEvent 2x where this fork fired 0 and 0,\nwhile the teleports themselves happened - so plugins that veto teleports or watch them are\nsimply never told.\nThe event is fired after the platform's own validity checks and before anything is mutated, on\nthe thread that owns the entity, so a cancel is a plain refusal with no lock to unwind, and\nsetTo is honoured including to another world. Teleports whose cause is UNKNOWN - the platform's\nown internal repositioning, which has no Paper counterpart - are skipped.\nDisabling this stops all three events again.",
  "lecithin.waypoint.comment": "Locator bar waypoints.",
  "lecithin.waypoint.remake_connections.comment": "Implement the waypoint manager's remakeConnections instead of throwing. It is not unused:\nServerScoreboard.updatePlayerWaypoint/updateTeamWaypoints call it on every team-membership or\nteam-colour change of an online player, because locator bar waypoints are team-coloured - so the\nvanilla /team command and any plugin team change killed the caller. It mirrors vanilla's\nServerWaypointManager and recreates the connection for every tracking player; createConnection\nalready hops to the player's owning region.\nDisabling this restores the throw, and with it the /team crash."
}
